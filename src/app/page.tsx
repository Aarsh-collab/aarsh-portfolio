"use client";
import { useEffect, useState } from "react";
import Image from "next/image";
import { archive, featuredProjects, type Project } from "@/data/projects";

const links = { github: "https://github.com/Aarsh-collab", linkedin: "https://linkedin.com/in/aarshdeep-singh-nandra-79b1042a3", email: "mailto:aarsh.singh2020@gmail.com" };
export default function Home() {
  const [caseStudy, setCaseStudy] = useState<Project | null>(null);
  const [filter, setFilter] = useState("All");
  const [scrollProgress, setScrollProgress] = useState(0);
  const filters = ["All", "AI / ML", "Research", "Full Stack", "Simulation", "Data", "Systems"];
  const projects = filter === "All" ? archive : archive.filter((p) => p[2].includes(filter));
  useEffect(() => {
    document.documentElement.classList.add("motion-ready");
    const sections = document.querySelectorAll<HTMLElement>("[data-reveal]");
    const observer = new IntersectionObserver((entries) => {
      entries.forEach((entry) => entry.isIntersecting && entry.target.classList.add("is-visible"));
    }, { threshold: 0.12 });
    sections.forEach((section) => observer.observe(section));
    const updateProgress = () => {
      const max = document.documentElement.scrollHeight - window.innerHeight;
      setScrollProgress(max > 0 ? window.scrollY / max : 0);
    };
    updateProgress();
    window.addEventListener("scroll", updateProgress, { passive: true });
    return () => { observer.disconnect(); window.removeEventListener("scroll", updateProgress); document.documentElement.classList.remove("motion-ready"); };
  }, []);
  return <main>
    <div className="scroll-progress" aria-hidden="true"><span style={{ transform: `scaleX(${scrollProgress})` }} /></div>
    <nav><a className="mark" href="#top">AN<span>.</span></a><div className="nav-links"><a href="#work">Work</a><a href="#research">Research</a><a href="#about">About</a><a href="#contact">Contact</a></div><a className="resume" href="/resume.pdf">Resume ↗</a></nav>
    <section id="top" className="hero wrap hero-reveal" data-reveal><div className="hero-copy"><p className="eyebrow">Tempe, Arizona · Mathematics (Statistics) @ ASU</p><h1>Aarsh<br />Nandra<span>.</span></h1><p className="role">Applied AI Engineer<br />&amp; Researcher</p><p className="intro">I build AI systems, research infrastructure, and intelligent products—from multi-agent simulations and multimodal evaluation systems to full-stack AI applications.</p><div className="actions"><a className="primary" href="#work">View my work →</a><a className="secondary" href={links.email}>Get in touch</a></div></div><SystemMap /></section>
    <section id="work" className="wrap section" data-reveal><div className="heading"><div><p className="eyebrow">Selected work</p><h2>Things I&apos;ve made<br />work in the real world.</h2></div><p>Five projects at the intersection of applied AI, research engineering, and product systems.</p></div><div className="cards">{featuredProjects.map((p, i) => <article className={`card c${i}`} key={p.title}><div className="meta"><span>{p.category}</span><small>{p.status}</small></div><div><h3>{p.title}</h3><p>{p.summary}</p></div><footer><div>{p.tags.slice(0, 3).map((tag) => <i key={tag}>{tag}</i>)}</div><button onClick={() => setCaseStudy(p)}>Case study →</button></footer></article>)}</div></section>
    <section id="research" className="research" data-reveal><div className="wrap research-grid"><div><p className="eyebrow">Research</p><h2>Research is where I learn to ask better technical questions.</h2></div><div><p className="big">At ASU&apos;s DaRL Lab, I work on educational AI evaluation infrastructure and the emerging problem of multi-agent classroom simulation—taking seriously both what a system does and how we know it works.</p><div className="notes"><div><b>EduCAST</b><p>Co-first author; AAAI manuscript in preparation. Multimodal evaluation pipelines for personalized educational-video generation.</p></div><div><b>Classroom simulation</b><p>Ongoing Concordia-based research into teacher-student interaction, interventions, and emergent patterns.</p></div></div></div></div></section>
    <section className="wrap section experience" data-reveal><p className="eyebrow">Engineering experience</p>{[["Aug 2026 — present","Applied AI Engineer","ASU Next Lab","Voice-first mobile AI prototype for dementia-caregiving research."],["Apr 2026 — present","Undergraduate Researcher","DaRL Lab · ASU","Multimodal evaluation and research infrastructure for educational AI."],["Feb 2025 — Jan 2026","AI Systems Engineer","Ender-IT","Recruiter-facing matching and workflow infrastructure."]].map((x) => <div className="job" key={x[1]}><time>{x[0]}</time><h3>{x[1]} <em>{x[2]}</em></h3><p>{x[3]}</p></div>)}</section>
    <section className="wrap section archive" data-reveal><div className="archive-head"><div><p className="eyebrow">Project archive</p><h2>More systems, experiments, and prototypes.</h2></div><div className="filters">{filters.map((x) => <button className={filter === x ? "on" : ""} onClick={() => setFilter(x)} key={x}>{x}</button>)}</div></div><div className="list">{projects.map((p) => <article key={p[0]}><div><h3>{p[0]}</h3><p>{p[1]}</p></div><div><span>{p[2]}</span>{p[3] ? <a href={p[3]} target="_blank" rel="noreferrer">GitHub ↗</a> : <small>Internal deployment</small>}</div></article>)}</div></section>
    <section className="wrap toolkit" data-reveal><div><p className="eyebrow">Technical toolkit</p><h2>Useful tools,<br />organized by the work.</h2></div><div className="tools">{[["AI & ML","LLM systems · multimodal AI · RAG · embeddings · vector search · Whisper · scikit-learn"],["Research & evaluation","experimental design · benchmarking · human evaluation · ablations · agent-based simulation · statistics"],["Product & backend","Python · FastAPI · REST APIs · SQLAlchemy · document processing · rate limiting"],["Frontend & mobile","React · React Native · Expo · TypeScript · Vite · Tailwind CSS"],["Data & infrastructure","SQL · PostgreSQL · Supabase · pandas · NumPy · synthetic datasets"],["Core languages","Python · JavaScript · TypeScript · Java · SQL"]].map((x) => <div key={x[0]}><h3>{x[0]}</h3><p>{x[1]}</p></div>)}</div></section>
    <section id="about" className="about" data-reveal><div className="wrap about-grid"><p className="eyebrow">About</p><div><h2>I like making complicated things concrete.</h2><p>I&apos;m a Mathematics (Statistics) student at Arizona State University&apos;s Barrett, The Honors College. I work across AI research and engineering because I enjoy understanding complex systems: how they behave, how they can be evaluated, and how to turn an ambiguous idea into software people can use.</p><p>I&apos;m especially interested in intelligent systems, simulation, machine learning, and difficult technical problems that reward careful reasoning.</p></div></div></section>
    <footer id="contact" data-reveal><div className="wrap"><p className="eyebrow">Contact</p><h2>Let&apos;s make something<br />worth testing.</h2><a className="email" href={links.email}>aarsh.singh2020@gmail.com →</a><div className="bottom"><span>© {new Date().getFullYear()} Aarsh Nandra</span><div><a href={links.github} target="_blank" rel="noreferrer">GitHub ↗</a><a href={links.linkedin} target="_blank" rel="noreferrer">LinkedIn ↗</a><a href={links.email}>Email ↗</a></div></div></div></footer>
    {caseStudy && <div className="overlay" onMouseDown={() => setCaseStudy(null)}><article className={`modal ${caseStudy.demo || caseStudy.research || caseStudy.mobile || caseStudy.verification ? "modal-featured" : ""}`} role="dialog" aria-modal="true" aria-labelledby="case-title" onMouseDown={(e) => e.stopPropagation()}><button className="close" onClick={() => setCaseStudy(null)} aria-label="Close case study">×</button><p className="eyebrow">{caseStudy.category} · {caseStudy.status}</p><h2 id="case-title">{caseStudy.title}</h2><p className="lead">{caseStudy.summary}</p>{caseStudy.demo ? <FeaturedCaseStudy project={caseStudy} /> : caseStudy.research ? <ResearchCaseStudy project={caseStudy} /> : caseStudy.mobile ? <MobileCaseStudy project={caseStudy} /> : caseStudy.verification ? <VerificationCaseStudy project={caseStudy} /> : <><div className="case-grid">{[["Context",caseStudy.detail.context],["What I built",caseStudy.detail.built],["Technical decisions",caseStudy.detail.decisions],["Current status",caseStudy.detail.status]].map((x) => <div key={x[0]}><h3>{x[0]}</h3><p>{x[1]}</p></div>)}</div>{caseStudy.github && <a className="primary" href={caseStudy.github} target="_blank" rel="noreferrer">Open GitHub ↗</a>}</>}</article></div>}
  </main>;
}
function SystemMap(){return <div className="map"><div><span>Working set</span><span>2025—now</span></div><svg viewBox="0 0 500 380" role="img" aria-label="Connected map of AI products, agents, evaluation, and research"><path d="M75 100c118 0 81 90 175 90s99-98 178-98M250 190c-97 0-65 103-175 103m175-103c100 0 91 110 181 110"/><path className="dash" d="M75 100c70 75 115 57 175 90M428 92c-77 48-95 67-178 98M75 293c76-77 105-70 175-103m181 110c-72-91-101-72-181-110"/><circle cx="250" cy="190" r="40" className="core"/><circle cx="75" cy="100" r="27"/><circle cx="428" cy="92" r="27"/><circle cx="75" cy="293" r="27"/><circle cx="431" cy="300" r="27"/><text x="250" y="185">BUILD</text><text x="250" y="201">SYSTEMS</text><text x="75" y="96">AI / ML</text><text x="75" y="112">PRODUCTS</text><text x="428" y="88">MULTI-</text><text x="428" y="104">AGENTS</text><text x="75" y="289">RESEARCH</text><text x="75" y="305">SYSTEMS</text><text x="431" y="296">EVALUATION</text><text x="431" y="312">&amp; DATA</text></svg><p>A practice built around ambiguous problems, rigorous evaluation, and working software.</p></div>}
function FeaturedCaseStudy({ project }: { project: Project }) {
  if (!project.demo) return null;
  return <>
    <div className="case-actions"><a className="primary" href={project.live} target="_blank" rel="noreferrer">Open live product ↗</a><a className="secondary case-secondary" href={project.github} target="_blank" rel="noreferrer">View source ↗</a></div>
    <section className="demo-block"><div className="demo-block-head"><div><p className="eyebrow">Product walkthrough</p><h3>The complete workflow, in context.</h3></div><a href={`https://www.youtube.com/watch?v=${project.demo.youtubeId}`} target="_blank" rel="noreferrer">Watch on YouTube ↗</a></div><div className="video-frame"><iframe src={`https://www.youtube-nocookie.com/embed/${project.demo.youtubeId}`} title="InsuranceAI product walkthrough" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" allowFullScreen /></div></section>
    <section className="case-story"><div><p className="eyebrow">The product</p><p>{project.detail.context}</p></div><div><p className="eyebrow">What I built</p><p>{project.detail.built}</p></div><div><p className="eyebrow">Built deliberately</p><p>{project.detail.decisions}</p></div></section>
    {project.architecture && <SystemFlow routes={project.architecture.routes} />}
    <section className="case-gallery"><div className="demo-block-head"><div><p className="eyebrow">Product evidence</p><h3>One flow, from visitor to broker.</h3></div><p>Demo uses fictional applicant details.</p></div><div className="gallery-grid">{project.demo.gallery.map((shot, index) => <figure className={`gallery-shot shot-${index + 1}`} key={shot.src}><div className="shot-image"><Image src={shot.src} alt={shot.alt} fill sizes={index === 0 ? "(max-width: 800px) 100vw, 60vw" : "(max-width: 800px) 100vw, 40vw"} /></div><figcaption><span>{shot.label}</span><p>{shot.description}</p></figcaption></figure>)}</div></section>
    <p className="case-disclaimer">{project.detail.status}</p>
  </>;
}
function SystemFlow({ routes }: { routes: { trigger: string; destination: string }[] }) {
  return <section className="system-flow"><div className="demo-block-head"><div><p className="eyebrow">System flow</p><h3>From a visitor&apos;s question to broker context.</h3></div><p>Each route preserves a reviewable, site-scoped record.</p></div><div className="flow-surface"><div className="flow-line"><span>Visitor</span><i>→</i><span>Public demo or embedded widget</span><i>→</i><span>React session + branding</span></div><div className="flow-line flow-infra"><span>Nginx + HTTPS</span><i>→</i><strong>FastAPI backend</strong><i>→</i><span>LLM intent router</span></div><div className="flow-routes">{routes.map((route) => <div key={route.trigger}><span>{route.trigger}</span><i>→</i><b>{route.destination}</b></div>)}</div><div className="flow-outcomes"><div><b>Life intake agent</b><span>Validated fields + state updates → ML preliminary pricing → SQLite</span></div><div><b>Life policy Q&amp;A agent</b><span>PDF parsing + extracted policy text → SQLite</span></div><div><b>Broker handoff agent</b><span>Contact + meeting request → broker email notification</span></div><div><b>Role-based admin dashboard</b><span>Brokers view only leads associated with their site ID</span></div></div></div></section>
}
function ResearchCaseStudy({ project }: { project: Project }) {
  if (!project.research) return null;
  return <><div className="case-actions"><a className="primary" href={project.github} target="_blank" rel="noreferrer">View evaluator source ↗</a></div><section className="research-intro"><div><p className="eyebrow">Research question</p><p>{project.detail.context}</p></div><div><p className="eyebrow">My contribution</p><p>{project.detail.built}</p></div><div><p className="eyebrow">Method</p><p>{project.detail.decisions}</p></div></section><section className="paired-run"><div className="demo-block-head"><div><p className="eyebrow">Documented paired run</p><h3>One lesson, two repair rounds.</h3></div><p>Internal archived comparison; scores are meaningful only within this evaluator configuration.</p></div><div className="video-pair">{project.research.videos.map((video, index) => <figure key={video.src}><div className="video-label"><span>{index === 0 ? "Before" : "After"}</span><b>{video.label}</b><small>{video.duration}</small></div><video controls preload="metadata" poster={video.poster}><source src={video.src} type="video/mp4" /></video><figcaption>{video.note}</figcaption></figure>)}</div></section><section className="scoreboard"><div><p className="eyebrow">Internal evaluator result</p><h3>4.000 <span>→</span> 4.375</h3><p>Overall score across the documented paired run.</p></div><div><strong>Visual Quality</strong><span>3 → 5</span><p>+2 after targeted visual repairs</p></div><div><strong>Logic / Instruction Flow</strong><span>4 → 5</span><p>+1 in the revised version</p></div><div><strong>Six remaining rubric dimensions were unchanged</strong><p>Scores were unchanged in this paired run</p></div></section><section className="comparison-evidence"><div className="demo-block-head"><div><p className="eyebrow">Evidence-backed revisions</p><h3>What the evaluation routed for repair.</h3></div></div>{project.research.comparisons.map((comparison) => <article key={comparison.timestamp}><div className="comparison-copy"><span>{comparison.timestamp}</span><p><b>Finding:</b> {comparison.finding}</p><p><b>Revision:</b> {comparison.revision}</p></div><div className="frame-pair"><figure><Image src={comparison.original} alt={`Original instructional-video frame at ${comparison.timestamp}`} fill sizes="(max-width: 800px) 100vw, 40vw" /><figcaption>Original</figcaption></figure><figure><Image src={comparison.revised} alt={`Evaluation-informed revised frame at ${comparison.timestamp}`} fill sizes="(max-width: 800px) 100vw, 40vw" /><figcaption>Revision</figcaption></figure></div></article>)}</section><section className="evaluation-flow"><div><p className="eyebrow">Evaluation loop</p><h3>Evidence first. Repair second.</h3></div><ol><li>Generated instructional video</li><li>Timestamped visual and audio evidence extraction</li><li>Structured lesson inference + rubric scoring</li><li>Evidence-backed feedback</li><li>Whole-plan or targeted visual repair</li><li>Revised video for controlled comparison</li></ol></section><section className="rubric"><div><p className="eyebrow">Eight-dimension rubric</p><h3>Built for instructional and visual quality—not a black-box score.</h3></div><p className="rubric-method">The rubric synthesizes instructional-video design research with Bloom&apos;s taxonomy and the ICAP engagement framework, translating them into observable 1–5 decision boundaries.</p><div className="rubric-tags">{project.research.rubric.map((metric) => <span key={metric}>{metric}</span>)}</div><p><b>Visual Quality example:</b> A 5 requires clear, readable, well-composed visuals with no meaningful defect; a 4 permits minor issues that do not impair learning; a 3 indicates a noticeable issue that harms readability or comprehension.</p></section><p className="case-disclaimer">{project.detail.status}</p></>;
}
function MobileCaseStudy({ project }: { project: Project }) {
  if (!project.mobile) return null;
  return <><div className="case-actions"><a className="primary" href={project.github} target="_blank" rel="noreferrer">View source ↗</a></div><section className="mobile-hero"><div className="phone-video"><video controls preload="metadata" poster={project.mobile.poster}><source src={project.mobile.video} type="video/mp4" /></video></div><div><p className="eyebrow">Design premise</p><h3>Make the next action obvious.</h3><p>{project.detail.context}</p><div className="mobile-principles"><span>Large touch targets</span><span>Readable text</span><span>User-triggered voice</span><span>Local sessions</span></div></div></section><section className="case-story mobile-story"><div><p className="eyebrow">What I built</p><p>{project.detail.built}</p></div><div><p className="eyebrow">System design</p><p>{project.detail.decisions}</p></div><div><p className="eyebrow">What works today</p><p>Text chat, on-device speech recording, server-side transcription, on-demand spoken replies, short-term context, and locally saved conversations.</p></div></section><section className="mobile-gallery"><div className="demo-block-head"><div><p className="eyebrow">Interaction evidence</p><h3>Accessible by design, not as an add-on.</h3></div></div><div>{project.mobile.screens.map((screen) => <figure key={screen.src}><div><Image src={screen.src} alt={`Dementia Assistant mobile screen showing ${screen.label.toLowerCase()} with large high-contrast controls.`} fill sizes="(max-width: 800px) 46vw, 23vw" /></div><figcaption><span>{screen.label}</span><p>{screen.description}</p></figcaption></figure>)}</div></section><section className="mobile-system"><div><p className="eyebrow">Bounded architecture</p><h3>Phone → backend → AI service.</h3></div><div><span>Expo / React Native</span><i>→</i><span>FastAPI</span><i>→</i><span>ASU CreateAI</span><p>Credentials stay on the backend. Chat sessions remain on the prototype device.</p></div></section><section className="prototype-boundaries"><p className="eyebrow">Prototype boundaries</p><div><p>Demo uses fictional profile data. The assistant does not provide medical advice, diagnoses, medication changes, emergency monitoring, or a replacement for caregivers or licensed clinicians.</p><p>Current AsyncStorage session storage is not encrypted. It must not be used for real patient information until security, authorization, retention, and compliance requirements are formally addressed.</p></div></section><p className="case-disclaimer">{project.detail.status}</p></>;
}
function VerificationCaseStudy({ project }: { project: Project }) {
  if (!project.verification) return null;
  return <><div className="case-actions"><a className="primary" href={project.github} target="_blank" rel="noreferrer">View source ↗</a></div><section className="verification-video"><div className="demo-block-head"><div><p className="eyebrow">Product walkthrough</p><h3>Verify provenance at creation time.</h3></div><a href={`https://www.youtube.com/watch?v=${project.verification.youtubeId}`} target="_blank" rel="noreferrer">Watch on YouTube ↗</a></div><div className="video-frame"><iframe src={`https://www.youtube-nocookie.com/embed/${project.verification.youtubeId}`} title="VoxProof product walkthrough" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" allowFullScreen /></div></section><section className="verification-story"><div><p className="eyebrow">The reframing</p><h3>Not “was this AI-generated?”</h3><p>VoxProof investigates whether text can be verified as having come through a trusted voice-input pipeline, rather than trying to infer authorship from the final text alone.</p></div><div><p className="eyebrow">What I built</p><p>{project.detail.built}</p></div><div><p className="eyebrow">Browser engineering</p><p>{project.detail.decisions}</p></div></section><section className="proof-flow"><div className="demo-block-head"><div><p className="eyebrow">End-to-end pipeline</p><h3>Voice becomes a verifiable text artifact.</h3></div></div><ol>{project.verification.steps.map((step, index) => <li key={step}><span>{String(index + 1).padStart(2, "0")}</span><p>{step}</p></li>)}</ol></section><section className="verification-details"><div><p className="eyebrow">Core mechanisms</p><h3>Built for the messy web, not one textbox.</h3></div><div className="verification-grid"><p><b>Capture + transcript</b> Browser MediaRecorder audio is uploaded as multipart data to FastAPI, where Whisper produces the transcript.</p><p><b>Proof transport</b> The backend signs the transcript; the extension maps the signature into invisible Unicode markers that travel with visible text.</p><p><b>Insertion</b> Last-focused-field tracking supports both standard inputs and rich-text <code>contenteditable</code> environments.</p><p><b>Live verification</b> TreeWalker, MutationObserver, cache checks, and badge guards scan dynamic page text without repeated verification work.</p></div></section><section className="security-notes"><p className="eyebrow">Research boundary</p><div><p>VoxProof verifies that text matches a transcript signed by the VoxProof pipeline. It does not establish speaker identity, eliminate replay risk, or determine whether a human wrote arbitrary text outside that pipeline.</p><p>Open questions include whether invisible signatures survive copy/paste, whether edited text correctly fails verification, platform stripping behavior, replay resistance, and stronger binding to time and platform context.</p></div></section><p className="case-disclaimer">{project.detail.status}</p></>;
}
